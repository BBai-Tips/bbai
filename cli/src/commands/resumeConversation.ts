import { Command } from "cliffy/command/mod.ts";

export const resumeConversation = new Command()
  .name("resume")
  .description("Resume a persisted conversation")
  .action(() => {
    // Implementation to be added
  });
