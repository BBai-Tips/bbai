import { Command } from "cliffy/command/mod.ts";

export const addFiles = new Command()
  .name("add")
  .description("Add files to the conversation")
  .action(() => {
    // Implementation to be added
  });
