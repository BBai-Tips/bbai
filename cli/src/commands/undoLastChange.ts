import { Command } from "cliffy/command/mod.ts";

export const undoLastChange = new Command()
  .name("undo")
  .description("Undo the last change")
  .action(() => {
    // Implementation to be added
  });
