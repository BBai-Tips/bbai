import { Command } from "cliffy/command/mod.ts";

export const runCommand = new Command()
  .name("run")
  .description("Run an arbitrary CLI command")
  .action(() => {
    // Implementation to be added
  });
