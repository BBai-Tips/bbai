import { Command } from "cliffy/command/mod.ts";

export const loadExternalContent = new Command()
  .name("load")
  .description("Load content from an external web site")
  .action(() => {
    // Implementation to be added
  });
